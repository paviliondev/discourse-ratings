{{#if showIncludeRating}}
  {{input type="checkbox" checked=includeRating}}
{{/if}}
<span>{{ratingTypeName}}</span>
<span>{{i18n 'composer.your_rating'}}</span>
{{#if includeRating}}
  {{star-rating enabled=includeRating rating=rating click=(action 'updateRating' rating)}}
{{/if}}
